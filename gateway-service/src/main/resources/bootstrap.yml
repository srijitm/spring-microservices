spring:
  application:
    name: gateway-service
  profiles:
    active: dev

---
spring:  
  profiles: dev
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://host.docker.internal:8769}
      failFast: true
